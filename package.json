{
  "name": "webgl_tests",
  "version": "1.0.0",
  "description": "some wegl junk code",
  "browser": "main.js",
  "scripts": {
    "build": "rm -fv build/* && npx tsc --noEmit && npx esbuild src/main.ts --bundle --loader:.ts=ts --sourcemap --outfile=build/bundle.js"
  },
  "author": "Tomaz Vieira",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.8.18",
    "typescript": "^4.1.2"
  },
  "dependencies": {
    "@types/gl-matrix": "^3.2.0",
    "gl-matrix": "^3.3.0"
  }
}
